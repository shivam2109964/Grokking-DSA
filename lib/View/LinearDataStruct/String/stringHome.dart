import 'package:flutter/material.dart';

class StringHome extends StatelessWidget {
  const StringHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("String home"),
      ),
    );
  }
}