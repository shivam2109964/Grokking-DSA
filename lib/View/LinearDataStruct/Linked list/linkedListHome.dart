import 'package:flutter/material.dart';

class Linkedlisthome extends StatelessWidget {
  const Linkedlisthome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Linked List"),
      ),
    );
  }
}